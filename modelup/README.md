# HaruharuRIM CAM model upload slot

Put uploaded HaruharuRIM CAM model JSON files here when you want the manual
GitHub Actions dispatch to generate code from a prebuilt local model.

Default path used by the workflow:

```text
modelup/haruharu-rim-cam-weights.json
```
